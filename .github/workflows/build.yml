name: Build and Test

on:
  push:
    branches-ignore:
      - 'deploy/prod'
  pull_request:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x
        
    - name: Restore dependencies
      run: dotnet restore src/Tedd.TUI.sln
      
    - name: Build
      run: dotnet build src/Tedd.TUI.sln --no-restore
      
    - name: Test
      run: dotnet test src/Tedd.TUI.sln --no-build --verbosity normal
