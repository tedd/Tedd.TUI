<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>preview</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GeneratePackageOnBuild>false</GeneratePackageOnBuild>

    <!-- NuGet Metadata -->
    <PackageId>Tedd.TUI.Imaging</PackageId>
    <Authors>Tedd</Authors>
    <Description>Image decoding and resolution adapters for Tedd.TUI (uses SixLabors.ImageSharp).</Description>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageTags>tui;console;terminal;markdown;image;ascii</PackageTags>
    <RepositoryUrl>https://github.com/Tedd/Tedd.TUI</RepositoryUrl>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Tedd.TUI\Tedd.TUI.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="SixLabors.ImageSharp" Version="3.1.11" />
  </ItemGroup>

</Project>
